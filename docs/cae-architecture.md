# Диплом-Инж · Архитектура: основной сайт + CAE-продукт (SSO)

> **Версия:** 0.2 (финальный черновик с решением по SSO)
> **Дата:** 2026-05-24
> **Состав:** маркетинговый сайт `диплом-инж.рф` + CAE-продукт `cae.диплом-инж.рф` + общий SSO-кабинет `id.диплом-инж.рф`
> **Стек целевой:** React + R3F + Vite на фронте, Python 3.11 Cloud Functions (NumPy/SciPy) на бэке, PostgreSQL для данных, S3 для проектов и отчётов
> **Базовая теория CAE:** Timoshenko Beam FEM для 3D-рам

---

## 1. Бизнес-контекст и стратегия

### 1.1. Продуктовая линейка

Три связанные системы под одним брендом и одним аккаунтом пользователя:

1. **`диплом-инж.рф`** — маркетинговый сайт + блог + страница тарифов наставничества + ЛК студента и наставника (заявки, расписание, файлы, чат, оплата).
2. **`cae.диплом-инж.рф`** — облачный CAE на Timoshenko FEM, проекты пользователя, биллинг подписок.
3. **`id.диплом-инж.рф`** — единая система авторизации (Single Sign-On), JWT, OAuth2 Authorization Code Flow. Логин один раз — доступ во все продукты.

**Перекрёстная лидогенерация:**
- В CAE есть кнопка «Нужна помощь наставника?» → автоматически создаётся лид в Битрикс24 + email уведомление (используется существующая функция `create-lead`, пользователь уже авторизован — поля заполняются автоматически).
- На основном сайте — баннер «Считай ВКР в нашем облачном CAE» с deep-link в `cae.` (если пользователь авторизован — попадает сразу в редактор, если нет — на страницу регистрации с описанием продукта).

### 1.2. Аудитория и тарифы

| Тариф | Цена | Целевая аудитория | Лимиты |
|-------|------|-------------------|--------|
| **Демо (без регистрации)** | 0 ₽ | Холодный трафик: студенты, инженеры на пробе | 1 расчёт в день на IP+fingerprint, до 10 элементов, без сохранения, водяной знак в PDF |
| **Базовый** | 590 ₽/мес или 200 ₽ за разовый расчёт | Студенты ВКР | 5 проектов, до 50 элементов, эпюры, PDF без водяного знака |
| **Продвинутый** | 1490 ₽/мес | Аспиранты, частная практика | 50 проектов, до 500 элементов, экспорт в DXF/STEP-обзор, библиотека ГОСТ-профилей расширенная |
| **Профи** | 3490 ₽/мес | Действующие инженеры, малые КБ | Безлимит проектов, до 5000 элементов, командный доступ (до 3 чел.), приоритетный solver-pool, нелинейный расчёт (опц.) |

### 1.3. Защита бесплатного режима от обхода
- **Layered identity:** сочетание IP, FingerprintJS Pro, временного browser-cookie с server-signed JWT и hash локального storage.
- **Лимит**: один расчёт демо в сутки на сочетание `(ip_hash, fingerprint_hash)`. История попыток хранится в `demo_attempts` (PostgreSQL) c TTL 30 дней.
- **Защита от VPN:** проверка IP по базе известных VPN/proxy/Tor exit-нод (например, `ipapi.is`, `vpnapi.io` через бэкенд) — при детекте VPN расчёт идёт, но в шапке PDF пишется «без верификации» (мягкое давление).
- **Капча:** Yandex SmartCaptcha (российский провайдер, ровно ложится в стек) перед запуском solver-а в демо-режиме.
- **Rate-limit:** на уровне backend-функции — не более 3 попыток рассчитать за 5 минут с одного IP.

Эта связка не убьёт настоящих пользователей, но повысит цену обхода до уровня, когда проще купить подписку за 590 ₽.

---

## 2. Системная архитектура

### 2.1. Высокоуровневая диаграмма

```
┌────────────────────────────────────────────────────────────────┐
│ Browser (React 18 + Vite + R3F + TailwindCSS)                  │
│ ┌────────────┐  ┌─────────────┐  ┌────────────┐  ┌──────────┐  │
│ │ AuthGate   │→ │ Project list│→ │ 3D Editor  │→ │ Report    │ │
│ │ (JWT, FP)  │  │ (CRUD)      │  │ (R3F canvas)│ │ (PDF/HTML)│ │
│ └────────────┘  └─────────────┘  └────────────┘  └──────────┘  │
└────────────────────────────────┬───────────────────────────────┘
                                 │ HTTPS / JSON
                                 ▼
┌────────────────────────────────────────────────────────────────┐
│ API Gateway: Cloud Functions Python 3.11                       │
│                                                                │
│  cae-auth        — регистрация, login, JWT, demo-fingerprint   │
│  cae-projects    — CRUD проектов, версии, обмен (share)        │
│  cae-solver      — приём JSON-модели, FEM-расчёт, ответ        │
│  cae-profiles    — справочник ГОСТ-сечений (двутавры, швеллеры)│
│  cae-report      — рендер PDF/HTML, сохранение в S3            │
│  cae-billing     — Robokassa/Юкасса, подписки, квоты           │
│  cae-lead        — лид в Битрикс24 + email (из CAE → осн.сайт) │
└────┬─────────────┬─────────────┬─────────────────┬─────────────┘
     │             │             │                 │
     ▼             ▼             ▼                 ▼
┌─────────┐  ┌──────────┐  ┌──────────┐  ┌─────────────────────┐
│ Postgres│  │ S3 bucket│  │ Yandex   │  │ External APIs:       │
│ (data)  │  │ (PDF,    │  │ SmartCap │  │ Robokassa, vpnapi.is │
│         │  │  models) │  │ tcha     │  │ Битрикс24 webhook    │
└─────────┘  └──────────┘  └──────────┘  └─────────────────────┘
```

### 2.2. Поток данных «от модели до отчёта»

1. **Пользователь** рисует раму на R3F-канве: точки → узлы (Nodes), линии → элементы (Elements). К узлам цепляются закрепления (BC), к элементам — материалы, сечения и нагрузки.
2. **Frontend** сериализует модель в JSON по схеме `SolverPayload` (см. §4.1) и валидирует локально (Zod-схема).
3. **POST `/cae-solver`** с JWT в заголовке. Backend:
   - Валидирует payload (Pydantic 2.x).
   - Проверяет квоту тарифа (число элементов, расчётов в день).
   - Запускает `TimoshenkoFrameSolver` (NumPy/SciPy).
   - Возвращает `SolverResponse` (см. §4.2).
4. **Frontend** строит эпюры в R3F (вспомогательные сцены для N, Q, M), деформированную форму (масштабируемая визуализация перемещений), карту σ_eq по Мизесу.
5. **Пользователь** отмечает чекбоксами разделы для отчёта → POST `/cae-report` → бэкенд рендерит HTML через Jinja2 и PDF через WeasyPrint, загружает в S3 → возвращает CDN-ссылку.

### 2.3. Личный кабинет и хранение проектов (CAE)

- **Authentication через SSO:** все запросы к `cae-*` функциям несут JWT, выданный `id.диплом-инж.рф` (см. §2bis).
- **Проект** = запись в таблице `cae_projects` (id, owner_id → SSO user_id, name, created_at, updated_at, current_version_id) + версии модели в `cae_project_versions` (jsonb-снимок).
- **Автосохранение** каждые 30 сек: PATCH `/cae-projects/{id}/draft` (overwrite draft-версии). Полное сохранение версии — по явному действию пользователя.
- **Share-ссылки:** генерируются как nanoid в `cae_project_shares`. Доступ read-only по public link с правом «копировать в свой ЛК».

---

## 2bis. SSO-архитектура (`id.диплом-инж.рф`)

### 2bis.1. Зачем выделять SSO

Без SSO у пользователя было бы два разных аккаунта (на маркетинговом сайте и в CAE), две оплаты, два пароля. С SSO — один логин и сквозной customer journey:

> Зашёл на основной сайт → почитал блог → оставил заявку на наставничество → зарегистрировался → попал в ЛК студента с расписанием встреч → перешёл по deep-link в CAE → посчитал свой узел → вернулся в ЛК → оплатил подписку → видит и наставничество, и CAE в одном счёте.

Это резко повышает LTV: средний клиент покупает 2 продукта вместо 1, а отток снижается за счёт привычки.

### 2bis.2. Технический протокол

**OAuth 2.0 Authorization Code Flow с PKCE** — индустриальный стандарт. Поток:

```
[user] → cae.диплом-инж.рф
        ↓ (не залогинен)
[user] → id.диплом-инж.рф/authorize?client_id=cae&redirect_uri=...&code_challenge=...
        ↓ (логин)
[id]   → редирект на cae.диплом-инж.рф/auth/callback?code=XYZ
        ↓
[cae]  → POST id.диплом-инж.рф/token { code, code_verifier }
[id]   → { access_token (JWT, 15 мин), refresh_token (90 дней), id_token }
        ↓
[cae]  → сохраняет refresh в httpOnly Secure SameSite=Lax cookie
        ↓
[cae]  → все запросы к cae-api с Authorization: Bearer <access>
```

Тот же поток для основного сайта `диплом-инж.рф` с `client_id=main`. После одного логина оба клиента имеют общий refresh-токен (через cookie на корневом домене `.диплом-инж.рф`).

### 2bis.3. JWT-структура

```json
{
  "sub": "user_01H7XK...",
  "email": "ivan@example.ru",
  "name": "Иван Петров",
  "roles": ["student"],
  "subscriptions": {
    "mentoring": { "tier": "individual_1m", "until": "2026-06-24" },
    "cae":       { "tier": "pro", "until": "2026-07-24" }
  },
  "iat": 1779635000,
  "exp": 1779635900,
  "iss": "https://id.диплом-инж.рф",
  "aud": ["main", "cae"]
}
```

Поле `aud` (audience) контролирует, к каким клиентам валиден токен. Поле `subscriptions` агрегирует подписки на оба продукта — фронт может сразу показать пользователю статусы.

### 2bis.4. Cloud Functions для SSO

| Функция | Назначение |
|---------|-----------|
| `sso-authorize` | OAuth start: проверка клиента, рендер логин-формы |
| `sso-token` | OAuth token exchange: `code → access + refresh + id_token` |
| `sso-refresh` | Обновление access-токена по refresh |
| `sso-userinfo` | Стандартный OAuth-эндпоинт `/userinfo`, выдаёт профиль |
| `sso-account` | Управление профилем, паролем, 2FA |

5 функций для SSO + 9 для CAE + текущие 5 для основного сайта = **19 функций суммарно**. Это много — план Б: объединить родственные через router в одной функции (опыт `get-contacts → articles+sitemap` показал, что это нормально работает).

### 2bis.5. Доменная инфраструктура

| Домен | Назначение | Проект poehali.dev |
|-------|-----------|---------------------|
| `диплом-инж.рф` | Маркетинговый сайт + ЛК-студент + ЛК-наставник | Существующий |
| `cae.диплом-инж.рф` | CAE-редактор и личный кабинет проектов | Новый |
| `id.диплом-инж.рф` | SSO-эндпоинты OAuth2 | Новый (или вынести в основной) |

Cookies SSO ставятся на `.диплом-инж.рф` (с точкой) — тогда обе системы видят refresh-токен.

### 2bis.6. Роли пользователя

| Роль | Назначение | Доступ |
|------|-----------|--------|
| `student` | Студент-наставленец | ЛК с расписанием, файлами, чатом, оплатой; CAE по своему тарифу |
| `mentor` | Наставник-инженер | Кабинет наставника со списком студентов, расписанием, выпиской заработка |
| `admin` | Команда диплом-инж.рф | Полный доступ ко всему |

Роли независимы от подписок: студент может быть владельцем CAE-Профи; наставник может быть автором статей блога.

---

## 3. База данных

### 3.1. Общие SSO-таблицы (БД проекта `id.диплом-инж.рф`)

```
sso_users
  id, email (unique), email_verified_at, password_hash (Argon2id),
  full_name, phone, avatar_url, locale, created_at, last_login_at,
  is_active

sso_user_roles
  user_id, role (student | mentor | admin), granted_at

sso_oauth_clients
  id, client_id, client_secret_hash, name, redirect_uris (jsonb),
  allowed_scopes (jsonb), is_active

sso_refresh_tokens
  id, user_id, client_id, token_hash, expires_at, revoked_at, user_agent, ip

sso_password_reset_tokens
  id, user_id, token_hash, expires_at, used_at

sso_2fa_secrets
  user_id, secret, backup_codes (jsonb), enabled_at
```

### 3.2. Таблицы основного сайта (`диплом-инж.рф`)

```
mentoring_subscriptions
  id, user_id → sso_users.id, tariff_id, started_at, expires_at,
  payment_id, status

mentoring_sessions
  id, student_id, mentor_id, scheduled_at, duration_min,
  status (planned | done | cancelled), zoom_link, notes

mentoring_files
  id, session_id | subscription_id, s3_url, filename, uploaded_by, created_at

mentoring_chat
  id, subscription_id, author_id, text, file_url, created_at
```

### 3.3. Таблицы CAE (`cae.диплом-инж.рф`)

Все поля `*user*` / `owner_id` / `student_id` / `mentor_id` ссылаются на `sso_users.id` (UUID). Между БД разных проектов poehali.dev таблицы НЕ связаны foreign-key напрямую — целостность поддерживается логически через SSO JWT.

```
cae_projects
  id, owner_id (→ sso_users.id), name, slug, created_at, updated_at,
  current_version_id, is_archived

cae_project_members
  project_id, user_id (→ sso_users.id), role (owner | editor | viewer), added_at

cae_project_versions
  id, project_id, model_jsonb, units, comment, created_at, created_by

cae_solver_runs
  id, project_id, version_id, owner_id, status (queued|running|ok|error),
  payload_jsonb, response_jsonb, duration_ms, error_text, created_at

cae_gost_profiles
  id, type (i_beam, channel, angle_eq, angle_uneq, hollow_rect, hollow_round, pipe, custom),
  designation (например "I20"), gost_ref, A, I_y, I_z, I_t, W_y, W_z, h, b, t_w, t_f, A_sy, A_sz,
  notes, source_pdf_url

cae_materials_library
  id, designation (Сталь Ст3, 09Г2С, 40Х...), E, G, nu, rho, sigma_T, sigma_B, sigma_-1,
  gost_ref, notes

cae_demo_attempts
  id, ip_hash, fp_hash, ua_hash, vpn_flagged, captcha_passed,
  payload_size_bytes, created_at

cae_tariffs
  id, slug, name, price_monthly, price_yearly, price_one_off,
  max_projects, max_elements, allow_pdf, allow_dxf, allow_step_view,
  allow_team, max_team_members, allow_nonlinear

cae_subscriptions
  id, user_id, tariff_id, started_at, expires_at, payment_id, status

cae_share_links
  id, project_id, slug, created_at, expires_at, allow_copy

cae_leads_from_cae
  id, user_id, scenario (need_mentor | need_pro_calc), comment,
  bitrix_lead_id, sent_to_email_at, created_at
```

Все денежные расчёты идут в копейках (INTEGER) во избежание FP-ошибок.

---

## 4. API-контракты (JSON-схемы)

### 4.1. `SolverPayload` — модель → solver

```json
{
  "meta": {
    "project_id": "proj_xxx",
    "version_id": "ver_xxx",
    "units": {
      "length": "m",
      "force": "N",
      "stress": "Pa",
      "moment": "N*m"
    },
    "global_axes": "right_handed_z_up"
  },
  "materials": [
    {
      "id": "m_steel_3",
      "name": "Сталь Ст3",
      "E": 2.06e11,
      "G": 7.92e10,
      "nu": 0.3,
      "rho": 7850,
      "sigma_yield": 245e6,
      "sigma_ultimate": 380e6
    }
  ],
  "sections": [
    {
      "id": "sec_I20",
      "name": "Двутавр I20 ГОСТ 8239-89",
      "type": "i_beam",
      "geometry": { "h": 0.200, "b": 0.100, "t_w": 0.0052, "t_f": 0.0084 },
      "A": 26.8e-4,
      "I_y": 1840e-8,
      "I_z": 115e-8,
      "I_t": 5.4e-8,
      "W_y": 184e-6,
      "W_z": 23.1e-6,
      "shear_area_y": 13.4e-4,
      "shear_area_z": 18.7e-4,
      "shear_coef_y": 0.5,
      "shear_coef_z": 0.7
    }
  ],
  "nodes": [
    {
      "id": "n1",
      "coords": [0.0, 0.0, 0.0],
      "label": "A"
    }
  ],
  "elements": [
    {
      "id": "e1",
      "type": "beam_timoshenko_3d",
      "node_start": "n1",
      "node_end": "n2",
      "material_id": "m_steel_3",
      "section_id": "sec_I20",
      "local_axis_orientation": {
        "method": "node_or_vector",
        "ref_vector": [0.0, 0.0, 1.0]
      },
      "releases_start": { "ux": false, "uy": false, "uz": false, "rx": false, "ry": false, "rz": false },
      "releases_end":   { "ux": false, "uy": false, "uz": false, "rx": false, "ry": false, "rz": false }
    }
  ],
  "boundary_conditions": [
    {
      "id": "bc1",
      "node_id": "n1",
      "type": "fixed",
      "constrained_dofs": ["ux","uy","uz","rx","ry","rz"]
    },
    {
      "id": "bc2",
      "node_id": "n5",
      "type": "pinned",
      "constrained_dofs": ["ux","uy","uz"]
    }
  ],
  "loads": [
    {
      "id": "L1",
      "case": "ULS_1",
      "type": "nodal_force",
      "node_id": "n3",
      "force": [0, 0, -10000],
      "moment": [0, 0, 0]
    },
    {
      "id": "L2",
      "case": "ULS_1",
      "type": "in_span_point",
      "element_id": "e2",
      "position_ratio": 0.5,
      "force_local": [0, 0, -5000]
    },
    {
      "id": "L3",
      "case": "ULS_1",
      "type": "distributed_uniform",
      "element_id": "e3",
      "load_local_per_length": [0, 0, -1500]
    },
    {
      "id": "L4",
      "case": "ULS_1",
      "type": "distributed_trapezoidal",
      "element_id": "e4",
      "load_start_local": [0, 0, -1000],
      "load_end_local":   [0, 0, -3000]
    }
  ],
  "load_combinations": [
    { "id": "ULS_1", "factors": { "DL": 1.35, "LL": 1.5 } }
  ],
  "analysis_options": {
    "type": "linear_static",
    "nonlinear": {
      "geometric": false,
      "physical_plasticity": false,
      "max_iterations": 30,
      "tolerance": 1e-4,
      "load_steps": 10
    },
    "diagram_subdivisions": 20,
    "include_self_weight": true,
    "gravity_vector": [0, 0, -9.80665]
  }
}
```

### 4.2. `SolverResponse` — solver → frontend

```json
{
  "status": "ok",
  "solver_version": "0.1.0",
  "duration_ms": 187,
  "summary": {
    "n_nodes": 12,
    "n_elements": 18,
    "n_dofs": 72,
    "max_displacement_mm": 4.21,
    "max_rotation_rad": 0.00213,
    "max_von_mises_MPa": 156.3,
    "min_safety_factor": 1.57
  },
  "nodal_displacements": [
    {
      "node_id": "n3",
      "ux": 0.00012, "uy": -0.00041, "uz": -0.00421,
      "rx": 0.00021, "ry": 0.0,      "rz": 0.0
    }
  ],
  "reactions": [
    {
      "node_id": "n1",
      "fx": 0, "fy": 0, "fz": 25000,
      "mx": 0, "my": 1500, "mz": 0
    }
  ],
  "elements": [
    {
      "element_id": "e1",
      "diagrams": {
        "x_points":     [0.00, 0.15, 0.30, 0.45, 0.60, 0.75, 0.90, 1.05, 1.20],
        "N":            [...],
        "Qy":           [...],
        "Qz":           [...],
        "T":            [...],
        "My":           [...],
        "Mz":           [...],
        "sigma_normal": [...],
        "tau_shear":    [...],
        "sigma_vm":     [...]
      },
      "max_values": {
        "abs_N_max": 24500.1, "abs_M_max": 8200.3,
        "abs_sigma_vm_max": 156.3e6, "safety_factor_min": 1.57
      }
    }
  ],
  "warnings": ["Элемент e7: касательные напряжения > 0.6·τ_yield, рекомендуется усилить сечение"],
  "errors": []
}
```

### 4.3. Прочие эндпоинты

- `POST /cae-auth/register`, `/login`, `/refresh`
- `GET /cae-projects` — список (с пагинацией)
- `POST /cae-projects` — создать
- `GET /cae-projects/{id}` — получить с актуальной версией
- `PATCH /cae-projects/{id}/draft` — автосохранение
- `POST /cae-projects/{id}/versions` — фиксация версии
- `GET /cae-profiles?type=i_beam&q=20` — поиск ГОСТ-профилей
- `POST /cae-report` — генерация PDF/HTML
- `POST /cae-lead` — лид с CAE в основной сайт (Битрикс24 + email)
- `POST /cae-billing/checkout` — создание платежа, redirect на платёжный шлюз
- `POST /cae-billing/webhook` — приём callback от Robokassa/Юкасса

---

## 5. Mechanical Engine (концепция, без кода)

### 5.1. Базовый элемент — 3D Timoshenko beam, 2 узла × 6 DOF = 12 DOF

DOF на узел: `[u_x, u_y, u_z, θ_x, θ_y, θ_z]` в **глобальной СК**.
Элементная матрица собирается в **локальной СК** элемента (ось x вдоль оси балки), потом поворачивается матрицей трансформации `[T]` (12×12) в глобальную.

Параметр сдвига для каждой главной плоскости:

```
Φ_y = 12·E·I_z / (G·A_s_y·L²)
Φ_z = 12·E·I_y / (G·A_s_z·L²)
```

Влияние сдвиговой деформации входит как делитель `(1 + Φ)` в члены изгибной жёсткости. При `Φ → 0` Timoshenko ⇒ Bernoulli-Euler.

Структура элементной матрицы `K_e` (12×12) — стандартная блочная: осевая жёсткость `E·A/L`, крутильная `G·I_t/L`, два блока изгиба в плоскостях `xy` и `xz` с поправками Φ_y и Φ_z.

### 5.2. Сборка глобальной матрицы

- Глобальная `K` собирается через DOF-маппинг.
- Внутренние шарниры (`releases`) реализуются через статическую конденсацию соответствующих DOF на уровне элементной матрицы ДО трансформации.
- Внутрипролётные нагрузки сводятся к эквивалентным узловым силам и моментам через `f_eq = ∫ N(x)·q(x) dx`, где `N(x)` — функции формы Timoshenko (см. Reddy «Finite Element Method»).

### 5.3. Граничные условия

Используется **метод исключения уравнений** (вычёркивание строк/столбцов закреплённых DOF) — точнее и быстрее penalty-метода, не вносит численного шума. Для специальных случаев (наклонные роликовые опоры) — multi-point constraints через метод Лагранжа.

### 5.4. Решение

`scipy.sparse.linalg.spsolve` на разрежённой `K` (CSR-формат). Размер задачи в MVP до 5000 элементов × 12 DOF = 60 000 DOF — комфортно укладывается в 256 МБ и 5 сек на cloud function.

### 5.5. Восстановление внутренних усилий

Для каждого элемента:
1. Из глобальных перемещений извлекаем 12 узловых перемещений элемента.
2. Поворачиваем в локальную СК: `u_local = [T]·u_global`.
3. По функциям формы Timoshenko строим эпюры `N(x)`, `Q_y(x)`, `Q_z(x)`, `T(x)`, `M_y(x)`, `M_z(x)` в N точках.
4. Считаем напряжения:
   - `σ_normal(x) = N(x)/A ± M_y(x)/W_y ± M_z(x)/W_z` в характерных точках сечения.
   - `τ_shear(x) ≈ Q(x)·S(x) / (I·b)` или приближённо `Q/A_s`.
   - `σ_eq_VM = √(σ² + 3·τ²)` (плоская задача напряжений в стенке).

### 5.6. Численная стабильность и валидация

- **Smoke-тест:** консольная балка под точечной силой — сравнение с аналитическим решением. Сходимость по сетке.
- **Тест на сдвиг:** короткая консоль (L/h = 5) — Timoshenko должен дать большее перемещение, чем Euler-Bernoulli (на ~5–10%).
- **Тест на 3D-раму:** портал из 3 элементов под боковой нагрузкой — сравнение с SAP2000 / LIRA-САПР референсной моделью.
- **Тест на жёсткое тело:** малая нагрузка, проверка `K·u = 0` при отсутствии нагрузок и удалении 6 DOF (rigid body modes).

### 5.7. Нелинейные расчёты (тариф Профи, с MVP)

**Геометрическая нелинейность (P-Δ):**
- Метод итерационного решения Newton-Raphson с обновлением геометрической матрицы жёсткости `K_geom`.
- Полная касательная матрица `K_T = K_lin + K_geom(u)` пересчитывается на каждой итерации.
- Применение: высокие колонны под сжатием, длинные консоли, элементы под значительной нормальной силой.
- Параметры: `max_iterations` (по умолчанию 30), `tolerance` (1e-4 по норме невязки), `load_steps` (10 шагов нагружения).

**Физическая нелинейность (упругопластичность):**
- Модель материала: билинейная диаграмма σ-ε с упрочнением (Bilinear Kinematic Hardening) или идеальный упругопластический материал (Elastic-Perfectly Plastic) — выбор пользователя.
- Параметры в `material`: `sigma_yield`, `tangent_modulus_E_tan`, `hardening_type` (`bilinear` | `perfect`).
- На уровне сечения — модель пластических шарниров (concentrated plasticity) для рам по нормам Eurocode 3 / СНиП II-23-81* (только для справки, не нормативно).
- Алгоритм: incremental-iterative с return mapping в Гауссовых точках интегрирования сечения.

**Комбинированный режим:**
- В payload `analysis_options.nonlinear.geometric = true` и `physical_plasticity = true` — solver запускает полную нелинейную постановку.
- Время расчёта: задача 100 элементов — 5–15 сек, 1000 элементов — до 60 сек (нужен повышенный таймаут функции, см. §11).

**Валидация:**
- Бенчмарк Эйлеровой устойчивости стойки: критическая сила P_cr должна совпасть с теорией при выходе на нелинейную ветвь.
- Тест пластического изгиба двутавровой балки: момент пластификации `M_pl = σ_y · W_pl` совпадает с аналитикой.

---

## 6. Frontend-стратегия (R3F)

### 6.1. Структура сцены

```
<Canvas>
  ├── <Grid />               # бесконечная 3D-сетка XY + Z-up
  ├── <Axes />               # глобальные оси
  ├── <Snap />               # snap к сетке, к узлам, к серединам элементов
  ├── <NodesLayer>           # сферы радиуса R, hover/select
  ├── <ElementsLayer>        # цилиндры/линии по 2 точкам
  ├── <BoundaryLayer>        # 3D-символы опор (треугольники, штриховка)
  ├── <LoadsLayer>           # стрелки сил, дуги моментов, эпюры распред. нагрузок
  ├── <SectionVolumeLayer>   # extruded ГОСТ-профили (toggle ON/OFF)
  ├── <ResultsLayer>         # деформированная форма, эпюры N/Q/M, σ_VM heat-map
  └── <TransformControls />  # drag-and-drop узлов (gizmo)
</Canvas>
```

### 6.2. Режимы редактора

- **Draw mode:** клик → добавить узел, второй клик → элемент между узлами. Snap к сетке, к существующим узлам.
- **Edit mode:** выбор узла/элемента → панель свойств справа, gizmo для перемещения узла.
- **Load mode:** клик на узле/элементе → попап выбора типа нагрузки и параметров.
- **BC mode:** клик на узле → выбор типа опоры.
- **Result mode:** read-only, toggle между картой деформаций, эпюрами, картой напряжений.

### 6.3. Снэп и сетка
- Базовый шаг сетки 0.1 / 0.5 / 1.0 м (выбирается).
- Snap к узлу: радиус 8 px на экране.
- Snap к середине / трети / четверти элемента.
- Ortho-mode (Shift): движение только по осям.

### 6.4. Volumetric view

При переключении тумблера каждый элемент рендерится не линией, а 3D-mesh-ом, полученным экструзией 2D-контура своего ГОСТ-сечения вдоль вектора элемента. Это позволяет визуально проверить корректность ориентации главных осей и собираемости узлов.

### 6.5. Модульная система отчёта

`ReportComposer` — компонент с чекбоксами:

- [x] Исходная геометрия (таблицы узлов, элементов)
- [x] Материалы и сечения
- [ ] Глобальная матрица жёсткости (для образовательных целей)
- [x] Граничные условия и нагрузки
- [x] Максимальные перемещения и реакции
- [x] Эпюры N, Q, M
- [x] Карта эквивалентных напряжений
- [x] Выводы и коэффициенты запаса
- [ ] Приложение: полные данные узлов в CSV

Кнопка «Сформировать» → POST на `/cae-report` с массивом выбранных секций → бэкенд через **Jinja2** рендерит HTML по шаблонам, **WeasyPrint** конвертирует в PDF с правильной поддержкой кириллицы и формул (MathJax → SVG до экспорта).

Шаблон PDF в стиле пояснительной записки ВКР: рамка ГОСТ 2.104-2006, Times New Roman 14pt, нумерация формул, библиография в конце.

---

## 7. Биллинг и интеграции

- **Платёжный шлюз:** Robokassa или Юкасса (оба российские, есть готовое расширение poehali.dev).
- **Подписка:** ежемесячная автоматическая через рекуррентные платежи.
- **Разовый расчёт:** одноразовый чек, доступ к 1 проекту на 7 дней.
- **Битрикс24:** при нажатии «Нужна помощь наставника?» — webhook в Битрикс с заполненными полями `user_email`, `project_name`, `n_elements`, `тариф`.
- **Email через Яндекс 360 SMTP:** уведомления о платежах, о завершении расчёта, о приближении срока подписки.

---

## 8. Безопасность

- HTTPS only, HSTS, CSP, CORS на whitelist.
- JWT signed RS256, секрет в env-переменных Cloud Functions.
- Все пароли — Argon2id, salt + pepper.
- Rate-limit на solver и report (Redis-токены или PostgreSQL counters).
- Аудит-лог критичных операций (login, payment, project_delete).
- Защита от XSS в публикуемых PDF (sanitization комментариев пользователя).
- Защита от DoS: размер payload ≤ 2 МБ, число элементов ≤ tariff_limit.

---

## 9. Этапность реализации (Roadmap, обновлено с учётом SSO)

| Этап | Содержание | Сроки (оценка) |
|------|-----------|----------------|
| **M0** | Это ТЗ, утверждение | 0,5 нед. ✅ |
| **M1** | **SSO-инфраструктура:** sso_users, JWT HS256, register/login/refresh/userinfo/logout, OAuth Яндекс + Google + VK ID OneTap (скрыт временно). Email-верификация частично (ждёт DKIM/SPF). | ✅ Готово |
| **M2** | **ЛК студента/наставника на основном сайте:** профиль, подписки, расписание встреч, файлы, чат, оплата | ⏳ Отложено |
| **M3** | **Каркас CAE:** /cae лендинг с waitlist, /cae/projects ЛК-проектов (CRUD), БД `cae_projects`, `cae_project_versions`, `cae_solver_runs`, `cae_waitlist`, `cae_tariffs`. Решение: используем /cae внутри основного сайта, поддомен — позже. | ✅ Готово |
| **M4** | **Ядро решателя (линейная статика 2D/3D) + cae-solver cloud function**: балочные элементы с параметром сдвига Φ, эпюры N/Q/M/σ_VM, валидация против аналитики (консольная балка: погрешность <1%), демо без авторизации, лимит 10 эл. для незалогиненных, до 5000 эл. для платных. | ✅ Готово |
| **M5** | **2D-редактор на SVG**: draw-node / draw-element / select / BC (защ./шарн./каток) / узловые нагрузки / snap к сетке (0.1–1 м) / pan-zoom / сохранение модели в `cae_project_versions` / запуск решателя кнопкой «Посчитать» / визуализация эпюр N/Qy/Mz/σ и деформированной формы прямо на канве. Авто-сохранение run в `cae_solver_runs`. 3D R3F-редактор — следующим этапом. | ✅ Готово |
| **M6** | 3D R3F-редактор + улучшенная визуализация: эпюры N/Q/M в 3D, гизмо перемещения узлов, карта σ_VM heat-map | 2 нед. |
| **M7** | Volumetric view + библиотека ГОСТ-профилей (двутавры 8239-89, швеллеры 8240-97, уголки, трубы) | 1 нед. |
| **M8** | Report Composer + PDF-рендер (Jinja2 + WeasyPrint, ГОСТ-стиль) | 1 нед. |
| **M9** | Биллинг (Robokassa / Юкасса) + тарифы + квоты (общий счёт на наставничество + CAE) | 1,5 нед. |
| **M10** | Демо-режим CAE: FingerprintJS Pro + Yandex SmartCaptcha + vpnapi.io + rate-limit | 0,5 нед. |
| **M11** | Командный доступ CAE: модель Owner / Editor / Viewer (тариф Профи) | 1 нед. |
| **M12** | Нелинейный solver: P-Δ (геометрическая) + упругопластичность (билинейное упрочнение + perfect-plastic) | 4 нед. |
| **M13** | Упрощённый STEP-парсер на Python (CARTESIAN_POINT + EDGE_CURVE → wireframe) | 1,5 нед. |
| **M14** | QA: бенчмарки против LIRA/SAP2000 (линейка + нелинейка), нагрузочное тестирование, аудит SSO-безопасности | 1,5 нед. |
| **M15** | Beta-релиз закрытой группе наставников, сбор фидбэка | 1 нед. |
| **M16** | Публичный релиз, маркетинговая интеграция с диплом-инж.рф | — |

**Итого: ~26 недель работы** (≈6,5 месяцев одного разработчика). При параллельной работе бэка и фронта — 4–5 месяцев.

---

## 10. Финальные решения по архитектурным вопросам

| № | Вопрос | Решение | Влияние на план |
|---|--------|---------|------------------|
| 1 | Нелинейные расчёты | **Полный пакет в Профи сразу:** P-Δ (геометрическая) + упругопластичность (билинейное упрочнение + perfect-plastic) | M10, +4 нед. См. §5.7 |
| 2 | Командный доступ Профи | **Owner / Editor / Viewer** (стандарт индустрии — Figma, Notion) | M9, +1 нед. БД-таблица `cae_project_members` с полем `role` |
| 3 | STEP-импорт | **Упрощённый парсер на чистом Python** в M11. Без pythonocc, без VPS. Парсим `CARTESIAN_POINT`, `EDGE_CURVE`, `VERTEX_POINT` из STEP AP203/AP214 → wireframe сетка узлов и элементов. Достаточно для рам, не работает с B-Rep/поверхностями. | M11, +1,5 нед. |
| 4 | Сейсмика / динамика / строители | **НЕ делаем.** Фокус продукта — машиностроение. Решение о выделении строительного направления — после MVP по результатам обратной связи. | Убрано из Roadmap |
| 5 | Сертификация | **Обучающий + инженерный инструмент.** Без аттестации Росстандарта и ЯБОК. Дисклеймер во всех PDF-отчётах: «Расчёт выполнен для учебных и предварительных инженерных целей. Не является документом для государственной экспертизы.» Снимает юридические риски, не блокирует целевую аудиторию (студенты ВКР + действующие инженеры для предварительных оценок). | Дисклеймер в Report Composer (M6) |
| 6 | Локализация | **Только русский в MVP.** Целевой рынок — РФ + русскоязычное СНГ. Английский / китайский — после первых 100+ платящих клиентов. | Никаких i18n-библиотек в MVP, прямой ru-текст в JSX |
| 7 | Публичный API | **Отложен до v2.0.** В MVP — только внутренний API между фронтом и бэком (JWT). Публичный API + OAuth — после валидации продукта (100+ платящих). | Не влияет на MVP, освобождает 1+ нед. |

---

## 11. Технические требования к Cloud Functions

### 11.1. Полный список функций (3 проекта poehali.dev)

| Проект | Функция | Memory | Timeout | Назначение |
|--------|---------|--------|---------|-----------|
| **id** | `sso-authorize` | 256 МБ | 5 сек | OAuth start, рендер логин-формы |
| **id** | `sso-token` | 256 МБ | 5 сек | OAuth token exchange (code → access + refresh) |
| **id** | `sso-refresh` | 128 МБ | 5 сек | Обновление access по refresh-токену |
| **id** | `sso-userinfo` | 128 МБ | 5 сек | `/userinfo` для OAuth-клиентов |
| **id** | `sso-account` | 256 МБ | 10 сек | Управление профилем, паролем, 2FA |
| **main** | `create-lead` | 128 МБ | 10 сек | (есть) Лиды в Битрикс + email |
| **main** | `get-contacts` | 128 МБ | 10 сек | (есть) Контакты + статьи блога + sitemap |
| **main** | `get-tariffs` | 128 МБ | 10 сек | (есть) Тарифы наставничества |
| **main** | `create-vacancy` | 128 МБ | 10 сек | (есть) Форма вакансий |
| **main** | `track-visit` | 128 МБ | 10 сек | (есть) Трекинг визитов |
| **main** | `mentoring-account` | 256 МБ | 10 сек | ЛК студента/наставника: сессии, файлы, чат |
| **main** | `mentoring-billing` | 256 МБ | 15 сек | Robokassa: подписка на наставничество |
| **cae** | `cae-projects` | 256 МБ | 10 сек | CRUD проектов, версии, share-ссылки |
| **cae** | `cae-profiles` | 128 МБ | 5 сек | Справочник ГОСТ-профилей и материалов |
| **cae** | `cae-solver-linear` | 512 МБ | 30 сек | Линейная статика, до 5000 элементов |
| **cae** | `cae-solver-nonlinear` | 1024 МБ | 120 сек | Нелинейный solver, повышенный таймаут |
| **cae** | `cae-step-parser` | 512 МБ | 60 сек | Парсинг STEP до 10 МБ |
| **cae** | `cae-report` | 512 МБ | 30 сек | WeasyPrint PDF-рендер |
| **cae** | `cae-billing` | 256 МБ | 15 сек | Robokassa: подписка на CAE |
| **cae** | `cae-demo-check` | 128 МБ | 10 сек | Антифрод-проверка демо (FP + VPN + капча) |

**Итого 20 функций в 3 проектах:**
- `id.диплом-инж.рф` — 5 функций (SSO)
- `диплом-инж.рф` — 7 функций (5 текущих + 2 новых для ЛК)
- `cae.диплом-инж.рф` — 8 функций (CAE-ядро)

### 11.2. Разделение по проектам poehali.dev

Три проекта дают три плюса:
- **Независимый лимит функций** — каждый проект имеет свой лимит, не конкурируют.
- **Изоляция БД и секретов** — секрет SSO_JWT_PRIVATE_KEY лежит только в id-проекте.
- **Возможность раздельного деплоя** — обновление CAE не задевает основной сайт.

### 11.3. План экономии (если лимит функций ограничен)

Можно объединить родственные функции через query-параметр `?resource=`:
- `sso-authorize` + `sso-token` + `sso-refresh` + `sso-userinfo` + `sso-account` → одна функция `sso` (роутер по `?action=`).
- `cae-projects` + `cae-profiles` + `cae-demo-check` → одна `cae-data`.
- `cae-billing` + `mentoring-billing` → одна общая `billing` на основном проекте.

С таким объединением итого можно уложиться в **8–10 функций суммарно** при цене небольшой потери модульности.

---

## 12. Дисклеймер для отчётов (юридическая защита)

Текст, который автоматически вставляется в шапку каждого PDF-отчёта:

> **Внимание.** Настоящий расчёт выполнен в облачной CAE-системе «Диплом-Инж.CAE» и предназначен для учебных целей и предварительной инженерной оценки. Программное обеспечение не аттестовано Росстандартом и не имеет статуса средства измерений или поверочного расчётного комплекса. Окончательные проектные решения должны быть проверены аттестованным расчётным ПО (LIRA-САПР, SCAD, ANSYS, APM WinMachine) и согласованы с ответственным конструктором/нормоконтролёром организации-исполнителя.

Дисклеймер не отключается. На демо-расчётах добавляется водяной знак «DEMO» на каждой странице.

---

## 13. Готовность к этапу M1

Документ финализирован. Решены 8 архитектурных вопросов (7 исходных + SSO). Следующий шаг — этап **M1: SSO-инфраструктура (`id.диплом-инж.рф`)**.

### 13.1. Что должно быть готово до старта M1

- Создан новый проект poehali.dev под `id.диплом-инж.рф` (отдельный от текущего).
- Зарегистрирован поддомен `id.диплом-инж.рф` и привязан к этому проекту (DNS-запись).
- Подтверждён лимит Cloud Functions на Pro-тарифе (нужно минимум 5 функций для SSO-проекта).
- Решение по платёжному шлюзу: **Robokassa** (рекомендую) или Юкасса.
- Выбор email-провайдера для писем верификации/восстановления: уже настроенный Яндекс 360 SMTP `info@диплом-инж.рф`.

### 13.2. Состав работ M1 (2,5 нед.)

**Бэкенд:**
- БД: миграции для `sso_users`, `sso_user_roles`, `sso_oauth_clients`, `sso_refresh_tokens`, `sso_password_reset_tokens`, `sso_2fa_secrets`.
- Функции: `sso-authorize`, `sso-token`, `sso-refresh`, `sso-userinfo`, `sso-account`.
- Хеширование паролей: `argon2-cffi`.
- JWT: `pyjwt` с алгоритмом RS256, ключи в секретах проекта.
- Email-верификация: токены в `sso_password_reset_tokens` (переиспользуем таблицу), отправка через SMTP.
- Регистрация двух OAuth-клиентов в `sso_oauth_clients`: `main` (диплом-инж.рф) и `cae` (cae.диплом-инж.рф) с `client_secret`.

**Фронтенд:**
- Страницы на `id.диплом-инж.рф`: `/login`, `/register`, `/forgot-password`, `/reset-password`, `/verify-email`, `/oauth/authorize`, `/account`.
- Дизайн в стиле основного сайта (ГОСТ-эстетика, чертёжные элементы).
- Интеграция OAuth-клиента в основной сайт `диплом-инж.рф`: кнопка «Войти», обработка `/auth/callback`, хранение refresh-token в httpOnly cookie на `.диплом-инж.рф`.

### 13.3. Что появится у пользователя после M1

- Один аккаунт `email + пароль` для всего экосистемного бренда «Диплом-Инж».
- Кнопка «Войти» в шапке основного сайта.
- Профиль пользователя (имя, телефон, аватар).
- Восстановление пароля по email.
- Опциональная двухфакторная аутентификация (TOTP).
- Готовая авторизация — её сразу подхватят все следующие этапы (ЛК студента M2, CAE M3+).

После M1 каркас SSO можно протестировать на демо-сайте без реальных функций, и потом постепенно нанизывать на него все остальные продукты.

---