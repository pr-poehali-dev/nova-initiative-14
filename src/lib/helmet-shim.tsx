/**
 * Единая точка импорта Helmet — реэкспорт react-helmet-async.
 * Используется во всех страницах через `import { Helmet, HelmetProvider } from "@/lib/helmet-shim";`
 */
export { Helmet, HelmetProvider } from "react-helmet-async";
