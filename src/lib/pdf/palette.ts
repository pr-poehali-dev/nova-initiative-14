/**
 * Цветовая палитра PDF-отчёта CAE.
 *
 *  - ink     — основной цвет текста и линий (тёмно-индиго #1a1a2e)
 *  - thin    — приглушённые подписи, размерные линии (серо-фиолетовый)
 *  - accent  — акцент: нагрузки, реакции (терракотовый из чертёжной палитры)
 *  - N/Q/M   — эпюры: продольная (синий), поперечная (зелёный), момент (терракот)
 *  - grid    — сетка, рамки, разделители таблиц (светло-серый)
 *
 * Все цвета — кортежи [R,G,B] для jsPDF (doc.setDrawColor(...C.ink)).
 */
export type RGB = [number, number, number];

export const C: Record<"ink" | "thin" | "accent" | "N" | "Q" | "M" | "grid", RGB> = {
  ink: [26, 26, 46],
  thin: [90, 90, 120],
  accent: [192, 57, 43],
  N: [44, 62, 128],
  Q: [26, 138, 90],
  M: [192, 57, 43],
  grid: [220, 220, 215],
};
