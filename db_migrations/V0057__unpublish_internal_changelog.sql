-- Снимаем с публикации внутренние записи журнала версий CAE:
--  1.5.15 «Генератор рекламы» — админ-инструмент,
--  1.6.0  «Дорожная карта PLM» — внутренний документ.
-- Обычным пользователям эта информация не нужна, поэтому исключаем их из
-- публичного списка через флаг is_published = FALSE.
UPDATE t_p28138419_nova_initiative_14.cae_changelog
SET is_published = FALSE
WHERE version IN ('1.5.15', '1.6.0');